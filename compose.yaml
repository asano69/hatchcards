services:
  hashcards:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./config.toml:/hashcards/config.toml:ro
      - ./cards:/hashcards/cards
      - ./pb_data:/hashcards/pb_data
    environment:
      - TZ=Azia/Tokyo
