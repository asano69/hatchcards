services:
  go-hashcards:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./config.toml:/hashcards/config.toml:ro
      - ./example:/hashcards/example
      - ./data:/hashcards/data
    environment:
      - TZ=Azia/Tokyo
