services:
  hatchcards:
    image: ghcr.io/asano69/hatchcards:latest
    container_name: hatchcards
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - hatchcards.env
    volumes:
      - ./data:/hatchcards/pb_data
      - ./cards:/hatchcards/cards
      - ./hooks:/hatchcards/hooks # optional
      #- /etc/ssl/certs/root_ca.crt:/certs/root_ca.crt:ro # optional
    environment:
      - TZ=Europe/London
      #
      - HATCHCARDS_MASTER_KEY=${HATCHCARDS_MASTER_KEY}
      #
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=3000
      - DATA_ROOT=./cards
      - FSRS_TARGET_RECALL=0.9
      - FSRS_MIN_INTERVAL=1.0
      - FSRS_MAX_INTERVAL=256.0
      #
      - HATCHCARDS_MASTER_KEY=korR79hhBKw1tSfLGz2XmZaMstKdTreamQT5+VDlmBA=
      # Not implemented yet
      #- DRILL_CARD_LIMIT=0 # max cards per session, 0 = no limit
      #- DRILL_NEW_CARD_LIMIT=0 # max new cards per session, 0 = no limit
      #- DRILL_ANSWER_CONTROLS=full # "full" (Forgot/Hard/Good/Easy) or "binary" (Forgot/Good)
      #- DRILL_BURY_SIBLINGS=true # bury sibling cloze cards within a session

    #networks:
    #  - traefik_net
    #labels:
    #  - traefik.enable=true
    #  - traefik.http.routers.hatchcards.rule=Host(`hatchcards.app.internal`)
#volumes:
#  data:
#  cards:
#networks:
#  traefik_net:
#    external: true
